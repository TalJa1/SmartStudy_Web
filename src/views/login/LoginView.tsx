import { Container } from "@mui/material";
import "../../styles/login/loginPage.css";

const LoginView = () => {
  document.title = "Smart Login";
  return <Container className="fullView">LoginView</Container>;
};

export default LoginView;
